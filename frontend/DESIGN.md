# タイプクエスト 全体設計ドキュメント

「なぜこの形にしたか」を残すための設計ドキュメント。ストーリーモードの詳細設計は
[`app/story/DESIGN.md`](app/story/DESIGN.md) に分けてあるので、そちらを参照。
このファイルはアプリ全体（ルーティング・認証・試練の塔・共通バトルエンジン）を扱う。

新しいシステムを足すときは、まずここ（または該当する `DESIGN.md`）に設計を書き足してから
実装する運用にしたい。

## 1. 概要

Next.js App Router（v16.2.9、Tailwind CSS v4、TypeScript）で作る、日本語タイピングRPG。
バックエンド（Node.js/Express/TypeScript、`backend/`）を導入済み。認証・セーブデータ・
ランキングの設計・API一覧は [`backend/DESIGN.md`](../backend/DESIGN.md) を参照
（当初はGoで書く予定だったが、フロントと言語を揃えられるNode.js/TypeScriptに変更した）。

`frontend/AGENTS.md` に「このNext.jsは訓練データと違う破壊的変更が入っている前提で
`node_modules/next/dist/docs/` を見てから書け」という注意書きがあるので、Next.js固有の
APIを使う前は一度そこを確認すること。

## 2. 画面構成・ルーティング全体像

| ルート | 内容 | 実装状況 |
|---|---|---|
| `/` | タイトル画面。矢印キーでゲーム開始/ランキング/設定を選ぶ | 実装済み |
| `/home` | モード選択ハブ（3節） | 実装済み |
| `/login`, `/register` | ログイン・登録フォーム（3節） | 実装済み（backend/と連携する本物の認証） |
| `/practice` | 試練の塔・難易度選択＋チュートリアル（5節） | 実装済み |
| `/story` | ストーリーモード（→ `app/story/DESIGN.md`） | 第1章のみ実装済み |
| `/battle` | 試練の塔・ストーリー両方が使う共通バトル画面（6節） | 実装済み |
| `/game` | 「闘技場」画面の想定ルートだが、ホーム画面の闘技場カードは実際には
  `/battle` に飛ぶため、現状はどこからも遷移できない宙に浮いたページ | **スタブ**。今後、闘技場カードから
  ここに飛ぶよう繋ぎ直す予定 |
| `/ranking` | ランキング画面。`GET /api/scores/leaderboard`から取得して表示 | 実装済み |
| `/result` | 結果画面。バトル自体は既に自前のゲームオーバー画面を持っているので、ここに
  遷移する導線が無い | **スタブ**。使い道が無いので廃止候補 |
| `/setting` | 設定画面。ログイン中はユーザー名・メールアドレスを表示しログアウトできる、
  未ログインならログイン画面へ誘導する | 実装済み |

## 3. 認証・ログイン（`backend/`と連携する本物の実装）

`backend/`（Node.js/Express/TypeScript、詳細は[`backend/DESIGN.md`](../backend/DESIGN.md)）
と連携する。以前はlocalStorageに平文でemail/passwordを保存するだけの見せかけ実装だったが、
今は本物のアカウント管理に置き換えてある：

- 登録（`app/register/page.tsx`）：`POST /api/auth/register`を呼ぶ。成功するとcookie
  （httpOnly JWT）が発行され、その場でログイン状態になる（登録直後に`/login`を経由する
  必要はなく、`/home`へ直接進む）。
- ログイン（`app/login/page.tsx`）：`POST /api/auth/login`を呼ぶ。
- ホーム画面（4節）は`GET /api/auth/me`の成否でログイン状態を判定する
  （旧`isLoggedIn`のlocalStorageフラグはもう使わない）。
- ログアウト・アカウント情報表示は`/setting`（`app/setting/page.tsx`）に実装してある。
- パスワードは`bcrypt`でハッシュ化してサーバー側DBに保存し、平文比較はしない。
  複数アカウント（email重複不可）に対応している。
- `frontend/lib/api.ts`がバックエンドへのfetchをまとめた薄いクライアント
  （`credentials: "include"`必須。cookieでログイン状態を持ち回すため）。

**注意**：`alert()`はブラウザのネイティブモーダルで、開いている間ページの他の操作
（自動テストからのDOM読み取り含む）が完全にブロックされることを確認したため、
ログイン・登録フォームのエラー表示は`alert()`ではなくフォーム内のインラインメッセージに
してある。今後同種のフォームを足すときもこのパターンに揃えること。

## 4. ホーム画面とモード選択のゲート（`app/home/page.tsx`）

3つのモードカード：
- 旅に出る（ストーリー） → `/story`、`locked: true`
- 闘技場（オンラインモード表記） → `/battle`、`locked: true`。実体はオンライン対戦
  要素が無く、共通バトルエンジンにそのまま入るだけ（`mode`未指定＝`practice`扱い相当）
- 試練の塔 → `/practice`、`locked: false`（誰でも入れる）

加えて、右上に設定アイコン → `/setting` を置く予定（2節参照。誰でも入れるが、中身は
ログイン状態によって表示が変わる）。

`locked: true` のカードを選んだ状態でクリック/Enterすると、`isLoggedIn` でなければ
ログイン誘導のダイアログを出す。矢印キーでのカード選択、クリックでの選択の両方に
同じロックチェックが重複して書かれている。

## 5. 試練の塔（`app/practice/`）

- `page.tsx`：初級/中級/上級の3枚のカードから `/battle?mode=practice&level=easy|normal|hard`
  へ遷移するだけの薄い画面。初回訪問時は自動でチュートリアルダイアログが開く
  （`showTutorial` 初期値 `true`）。「あそびかた」ボタンでいつでも再度開ける。
  チュートリアル表示中は矢印キーでのカード選択を無効化している。
- `tutorial.tsx`：5枚組のカルーセル（embla-carousel）。①白/黒の単語色＝自分の攻撃/
  敵の攻撃の区別、②敵を倒すと残り時間が増える、③1/2/3キーで炎/氷/雷の魔法を選択
  （再押しで解除）、④階層が上がるほど単語が長く・速くなり5階ごとにボス、⑤階層ごとに
  宝箱でアイテムが手に入り、Enterを押すまでゲームが一時停止する、という流れを説明。
  **修正済みのバグ**：ダイアログに `max-w-2xl` を指定していたが、ベースの
  `DialogContent`（`components/ui/dialog.tsx`）が `sm:max-w-sm` を持っており、
  Tailwindが生成するCSSではレスポンシブ指定（`sm:`）が無指定のクラスより後ろに
  並ぶため、640px以上の画面では `sm:max-w-sm` が後勝ちしてダイアログが384pxまで
  縮み、カルーセルや文章が横にあふれていた。`className`側を `sm:max-w-2xl`
  （同じ`sm:`バリアントで指定）に直すことで、`tailwind-merge`が正しく後勝ちさせて
  くれるようになり解決した。同じ理由で他のダイアログの幅を広げたいときも、
  ベースのブレークポイント（`sm:`）に合わせて指定すること。
- `/battle` への引き継ぎは `mode`/`level` の2つのクエリだけ（ストーリーモードは
  これに加えて `hp`/`maxHp`/`words`/`attackBooks`/`defenseBooks`/`encounter` 等
  多数を渡す。試練の塔とストーリーで必要な情報量がそもそも違うことの表れ）。

## 6. 共通バトルエンジン（`app/battle/`）

試練の塔・ストーリーモードの両方が同じ `battle/page.tsx` を共有し、`mode`
（`"practice"` | `"story"`）で随所を分岐する（`isStory` フラグ）。ストーリー
モード固有の部分（HP持ち越し、単語プールの分離、コトの復活演出、言葉ドロップなど）は
[`app/story/DESIGN.md`](app/story/DESIGN.md) の6〜7節にまとめてあるので、
ここでは**試練の塔側の一般的な仕組み**を書く。

### 6.1 階層進行
`floor` は1から始まり、フロアの敵を全滅させると `grantTreasure()`（6.2節）を呼んで
一時停止し、Enterで次の階層へ。5階ごと（`isBossFloor`）はボス階になり、`enemyList.boss`
からランダムで1体だけ出現、HP/スコア/攻撃力に`BOSS_HP_MULTIPLIER`(2)/
`BOSS_SCORE_MULTIPLIER`(4)/`BOSS_ATTACK_MULTIPLIER`(1.2)がかかる。ボス階以外は
`getEnemyCountForFloor`（3階ごとに1体増、最大3体）、`getEnemyAttackDamage`
（1階につき+8%）、`getWordSpeedForFloor`（1階につき+5%）でじわじわ強くなる。
出てくる単語の長さ分布も `pickWordForFloor` が階層に応じてガウス重み付けで
長い単語のほうへシフトさせる（5段階の長さティア。ストーリーモードもこの関数を
流用している＝6節の設計思想が全体で共有されている）。

### 6.2 宝箱・アイテム
階クリアごとに6種類（`attack`/`defense`/`hp`/`fireMagic`/`iceMagic`/`lightningMagic`）
から1つランダムで付与（`grantTreasure`）。attack/defenseは倍率を`ITEM_ATTACK_BONUS`/
`ITEM_DEFENSE_BONUS`(共に0.1)ずつ強化（defenseは`ITEM_DEFENSE_MULTIPLIER_MIN`(0.3)で
下限あり）、hpは`ITEM_HP_BONUS`(20)ぶんさいだいHPを上げて全回復、魔法系はその魔法の
チャージを1つ増やす。Enterが押されるまでゲーム（単語のスクロール・残り時間）を
完全に一時停止する。

### 6.3 魔法システム
敵種族ごとに固定の`dropsMagic`があり、撃破時に`MAGIC_DROP_CHANCE`(0.35)の確率で
その魔法を1チャージ入手（ボスは確定ドロップ）。自分の攻撃フェーズ中のみ1/2/3キーで
発動する魔法を選択（再度押すと解除）。炎＝ダメージ`FIRE_DAMAGE_MULTIPLIER`(2倍)、
氷＝次の3単語ぶん流れる速度を`ICE_SLOW_SPEED_MULTIPLIER`(0.4倍)に、雷＝生存中の
敵全員に同時ダメージ（`resolveLightningAttack`）。魔法使用時は会心が出ない。

**今後の予定**：光魔法・回復魔法などの追加。追加する際は`MagicType`・`MAGIC_META`・
`ITEM_TYPES`（宝箱ドロップ対象）・チュートリアルの説明文の4箇所を揃えて直すこと。

### 6.4 コンボ・会心・ミスペナルティ
単語を打ち切るたびにコンボが+1、時間切れでリセット。会心率`getCritChance`は
コンボ×0.03（上限50%）、会心ダメージは`CRIT_DAMAGE_MULTIPLIER`(1.5倍)。ミスは
その「ターン」内での回数に応じて`getMissPenaltyMultiplier`でダメージ/防御効果を
弱める（階層が上がるほど1ミスの重みが増す、最低でも1割は残る）。単語自体の
「威力」は`getWordPower(word) = length * (length + 5)`（文字数の2乗に比例させて、
短い単語の連打が長い単語より強くならないようにしてある。この式の設計理由は
`battle/difficulty.ts`内のコメントに詳しい）。

### 6.5 ローマ字入力エンジン（`romaji.ts`）
`ROMAJI_TABLE`が各かな（拗音は2文字セットも）を許容するローマ字表記の配列に
マッピングしている（先頭が既定表示）。`buildWordUnits`がかな単語を「打鍵単位」に
分解する際の工夫：
- 拗音（きゃ等）は1つの単位として消費
- 促音（っ）は単独の打鍵にせず、次の単位の子音を重ねる形にたたみ込む
  （「った」→ `["tta"]`）
- 「ん」は**ロールオーバー処理**が必要な特殊ケース：単語末尾なら`["n"]`だけで確定
  できる（後に続く文字が無く曖昧さが無いため）。次の単位がa/i/u/e/o/n/yで始まる
  （＝「な」なのか「んな」の続きなのか曖昧になりうる）場合は`["nn"]`のみを受け付け
  二重入力を強制する。それ以外は`["n","nn"]`両方を許容する。
  （このロールオーバー処理は、たんぽぽ等の実際の単語で過去にバグが出た経緯があり、
  ストーリーモードの単語収集で似た問題が再発しないよう、単語追加時は打鍵の
  ロールオーバーが正しく効くか実際に打って確認するとよい）

### 6.6 単語リスト（`words.ts`）
難易度ごとに `{kana, kanji}` の配列。短い単語〜長い単語まで幅広く含み、
`pickWordForFloor` の長さティア抽選で使われる。ストーリーモード専用の単語は
別ファイル（`app/story/storyWords.ts`）に分離してあり、こちらとは共有しない
（`app/story/DESIGN.md` 6節参照）。

### 6.7 時間制限
`TIME_BONUS_PER_KILL`（敵を1体倒すごとに+5秒）とは別に、`TIME_BONUS_PER_WORD`
（単語を1つ打ち切るごとに+1秒）がある。自分の攻撃・防御どちらのフェーズでも共通で、
単語を1つ「打ち終える」たびに必ず呼ばれる`advanceWord`関数の先頭で、`isTimeout`が
`false`（＝時間切れではなく実際に打ち切れた）ときだけ加算している。時間切れで
`advanceWord`が呼ばれたとき（`isTimeout: true`）は加算しない。

## 7. ストーリーモード

→ [`app/story/DESIGN.md`](app/story/DESIGN.md) を参照。マップ・当たり判定の自動計算、
会話・立ち絵システム、単語収集と難易度カーブ、アイテム経済、メニューUI、セーブ演出など
第1章の設計をまとめてある。

**大きな方針転換（ドラクエ方式に確定）**：章ごとに独立したマップを作るのをやめて、
村・草原などを含んだ1枚の全体マップを歩き回るRPGにしたい、という方針を最初に検討し、
一時期は村と草原を1枚の地続きマップ（`WORLD_MAP`）に物理統合していた。しかし
「本当にドラクエのように、城や町から出たら世界を冒険しているようなマップで縮図に
してほしい」という要望を受けて、村（詳細な1:1スケールの`TOWN_MAP`）とフィールド
（村より大きな縮尺の世界地図＝`FIELD_MAP`、いわば「縮図」）を**別マップ・別シーン**に
戻した。ドラクエで町に入ると町専用の詳細マップに切り替わるのと同じ考え方（具体的な
仕組みは [`app/story/DESIGN.md`](app/story/DESIGN.md) 4.2.1節を参照）。

村の門（`town-exit`）にぶつかるとフィールドへ、フィールドの村アイコン
（`field-town-entrance`）にぶつかると村へ、シーンごとテレポートする。必須の言葉
（みず・ひ・かぜ）を3つ集めるまでは村の門でコトに呼び止められて先に進めない――
これが「ボスを倒すごとに次のエリアへ行けるようになり、それまでは仲間の制止で
足止めする」という進行ゲートの実装パターンになっている（詳細は
[`app/story/DESIGN.md`](app/story/DESIGN.md) 8.1節を参照）。

### 7.1 8章構成の全体設計（正式版）

ドラクエ・ゼルダ・ポケモンが採用している「新しい能力を手に入れて行動範囲が広がる」
設計を踏襲する。各章は必ず **町 → フィールド → ダンジョン → ボス → 町へ帰還** の
流れを持ち、ボスを倒すと新しい能力（魔法・装備・システム）が解放され、それによって
次の章の探索・ギミックが可能になる。**各章で新しい実装を1つずつ増やす**、という
方針も明確にしてある（章一覧の「テーマ／新要素」列がそのまま各章で追加する機能）。

マップの詳細設計（ワールドマップとエリアマップの2種類の関係、各章のマップ構成・
物語の流れ）は [`app/story/WORLD_DESIGN.md`](app/story/WORLD_DESIGN.md) にまとめてある。

#### 章一覧

| 章 | エリア名 | テーマ／新要素 | ボス |
|---|---|---|---|
| 1 | はじまりの村 | チュートリアル（移動・タイピング・会話・宝箱・言霊の書） | スライムキング |
| 2 | 砂漠の町 | 防具購入（お金・ショップが初めて使える） | オークキング |
| 3 | 魔法の森 | 魔法を使えるようになる（ほのお/みず/かぜ→火球/水流/風刃、MP管理開始） | フェンリル |
| 4 | 謎の洞窟 | 魔法によるマップ攻略（炎でツタ焼却／風で風車／水で溶岩冷却） | ドラゴン |
| 5 | 荒れ果てた町 | 謎の青年の正体判明＋回復魔法（宿屋強化・状態異常回復） | 英雄（の亡霊、対話で決着） |
| 6 | 毒の沼地 | 毒によるマップ上の攻撃効果（地形ハザード）。回復魔法が活躍 | クラーケン |
| 7 | なぞをとけ（沈黙の塔） | 情報収集＋総合ダンジョン（今までの魔法・ギミック・タイピングを総動員し魔王大陸への道を探る） | （ボスなし、最後の英雄と対話） |
| 8 | 魔王大陸 | 魔王討伐（道中でゲットした光魔法で攻略、集大成） | ラスボス（一番長い言霊で決着） |

各章のエリア構成（町→フィールド→ダンジョン→ボス）、世界地図の一本道チェーン、
「言葉で世界が変わる」演出、ワールドマップ／エリアマップという2種類のマップの
関係性の技術的な仕組みは、すべて [`app/story/WORLD_DESIGN.md`](app/story/WORLD_DESIGN.md)
に詳細をまとめてある。

**実装済み**：1章の町・フィールド（`TOWN_MAP`/`FIELD_MAP`、詳細は
[`app/story/DESIGN.md`](app/story/DESIGN.md)）、`ChapterSelect.tsx`の8章タイトル
（上の表の名前に更新済み）、世界地図上の`FIELD_LANDMARKS`（次の行き先の目印）。

**まだ実装していないこと（着手時はそれぞれ個別に設計を詰めてから）**：
- ショップ・通貨システム（2章から必要）
- 魔法システム（詠唱＝言霊のタイピング、MP管理、3章から必要。試練の塔の魔法
  ドロップ・詠唱システム＝`battle/`側の`MagicType`を拡張する形になりそう）
- 地形ハザード・状態異常システム（毒等、6章から必要）
- ギミック攻略システム（魔法を使って謎を解く、4章・7章で必要）
- 2〜8章それぞれの町・フィールド・ダンジョンの実マップデータ
- 章クリアによるワールドマップの見た目変化（「言葉で世界が変わる」演出）

マップ構造の作り直しが絡む変更なので、次の章（2章）に着手する際は、まず
[`app/story/WORLD_DESIGN.md`](app/story/WORLD_DESIGN.md) 3節の進め方に沿って、
2章の具体的なショップUI設計等を書き足してから始めること。

## 8. 共通UIキット（`components/ui/8bit/`）

`badge` / `button` / `card` / `carousel` / `dialog` / `input` / `progress` の
8bit風（ドット絵アニメ）ラッパーコンポーネント群。shadcn/ui＋Radix UIの上に
レトロゲーム風の見た目（`DotGothic16`フォント、太い枠線）を被せてある。
`components/HomeButton.tsx` はどの画面からでも `/home` に戻れる共通ボタン。

## 9. 技術スタック

Next.js 16.2.9（App Router）／React 19.2.4／TypeScript 5／Tailwind CSS v4／
Radix UI／class-variance-authority・clsx・tailwind-merge（`cn()`ユーティリティ）／
embla-carousel-react（チュートリアルのカルーセル）。バックエンド通信は追加パッケージを
入れず、ネイティブの`fetch`を`lib/api.ts`で薄くラップして使っている（3節参照）。

## 10. 未実装・スタブ・今後の課題

- `/game`・`/result` は文字だけのスタブ画面のまま（2節に詳細と方針）。
  `/ranking`・`/setting`はbackend/導入に合わせて実装済み。
- ストーリーモードは第1章のみ。2〜8章は`ChapterSelect.tsx`に鍵付きプレースホルダーが
  あるだけでデータ未実装。7節の「村とフィールドを別マップにする」方針は第1章の
  範囲では実行済みだが、2章以降をフィールドの外側にどうつなげるかは未確定なので、
  着手する前にそちらの設計を先に固めたほうがよい。
- 魔法の種類追加（光・回復など、6.3節）は、まだ設計途中。
- スコア送信（`battle/page.tsx`）は現状ゲームオーバー時のみ（ストーリーの個々の雑魚戦
  勝利では送らない、3節参照）。ストーリーモードのランキングをもっと意味のある単位
  （章クリアタイムなど）で記録したくなったら、送信タイミングを見直すとよい。
